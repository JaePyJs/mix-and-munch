# ========================================
# Mix & Munch - Next.js Environment Example
# ========================================
# Copy to `.env.local` and fill the secrets before running locally.
# Files ending with .local are ignored by git.

# Gemini API key (primary AI model - Gemini 2.5 Pro recommended)
GEMINI_API_KEY=your_gemini_api_key_here

# GLM 4.6 API key (fallback AI model)
GLM_API_KEY=your_glm_api_key_here

# Use Gemini Flash instead of Pro (false = use Pro, true = use Flash)
USE_GEMINI_FLASH=false

# Optional: base URL used for generating canonical links
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: label shown in the UI footer
NEXT_PUBLIC_STUDIO_TAGLINE=Capstone Kitchen Studio
